{
  "name": "predictive-load-balancer",
  "version": "1.0.0",
  "description": "Predictive Load Balancer full stack application",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "cd frontend && npm run build && cp -r build ../backend/public",
    "start": "cd backend && npm start",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\"",
    "docker:build": "docker build -t predictive-load-balancer .",
    "docker:run": "docker run -p 5005:5005 predictive-load-balancer"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
